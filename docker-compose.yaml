version: '3.3'
services:
    web:
      container_name: web
      build: ./web
      command: gunicorn --timeout 100 --bind 0.0.0.0:5001  app:app
      volumes:
        - ./web/:/usr/src/app/
      ports:
        - 5001:5001
      restart: always
